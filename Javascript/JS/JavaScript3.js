//文字列と変数を連結して、二つの値を足し算し、出力例通りに出力する。

var FirstAmount  = 10;
var SecondAmount = 20;

//加算した結果を格納
var ResultAmount = FirstAmount + SecondAmount;

alert(FirstAmount + "+" + SecondAmount + "=" + ResultAmount);
